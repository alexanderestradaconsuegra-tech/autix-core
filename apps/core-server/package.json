{
  "name": "@autix/core-server",
  "version": "0.0.0",
  "private": true,
  "description": "Servidor HTTP entrante de Autix Core (Fastify) — expone el ExecutionEngine a agentes por red (RFC-000 §14).",
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@autix/contracts": "workspace:*",
    "@autix/core": "workspace:*",
    "@autix/schemas": "workspace:*",
    "@fastify/jwt": "^10.2.1",
    "fastify": "^5.10.0",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@autix/tsconfig": "workspace:*",
    "zod": "^4.4.3"
  }
}
